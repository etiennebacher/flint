# fun = function(one)
# {
#   one.plus.one <- oen + 1
#   four <- newVar <- matrix(1:10,nrow = 2)
#   four[ 1, ]
#   txt <- 'hi'
#   txt <<- 1
#   1 ->> txt
#   txt -> 1
#   three <- two+ 1
#   if(txt == 'hi') 4
#   5
# }

any(duplicated(x), na.rm = TRUE)

any(duplicated(x))

# sum(mean(x))

# sum(duplicated(x))

any(is.na(x))

apply(x, 1, sum)

apply(x, 1, paste)

apply(x, 2, sum)
